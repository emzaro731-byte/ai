{
  "name": "ai-api-key-server",
  "version": "1.1.0",
  "private": true,
  "description": "Cloudflare Workers API-key service for the AI/Flutter app",
  "main": "src/worker.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
